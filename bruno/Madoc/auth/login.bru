meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{host}}/auth/login
  body: formUrlEncoded
  auth: inherit
}

body:form-urlencoded {
  username: {{admin_username}}
  password: {{admin_password}}
}

settings {
  encodeUrl: true
}
