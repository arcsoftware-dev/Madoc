meta {
  name: upload roster
  type: http
  seq: 1
}

post {
  url: {{host}}/api/roster/upload
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  file: @file()
  year: {{year}}
}

vars:pre-request {
  year: 2025
}

settings {
  encodeUrl: true
}
