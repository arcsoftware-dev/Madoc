meta {
  name: Replace
  type: http
  seq: 2
}

post {
  url: {{host}}/api/constitution/replace
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  file: @file()
}

settings {
  encodeUrl: true
}
